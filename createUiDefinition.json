{
  "$schema": "https://schema.management.azure.com/schemas/0.1.2-preview/CreateUIDefinition.MultiVm.json#",
  "handler": "Microsoft.Azure.CreateUIDef",
  "version": "0.1.2-preview",

  "parameters": {
    "config": {
      "isWizard": false,
      "basics": {
        "subscription": {
          "constraints": {
            "required": true
          }
        },
        "resourceGroup": {
          "allowExisting": true
        },
        "location": {
          "visible": true
        }
      }
    },

    "basics": [],
    "steps": [
      {
        "name": "AlluxioClusterSettings",
        "label": "Alluxio Cluster Configuration",
        "elements": [
          {
						"name": "extensionResourceName",
						"type": "Microsoft.Common.TextBox",
						"label": "Cluster extension resource name",
						"defaultValue": "",
						"toolTip": "Only lowercase alphanumeric characters are allowed, and the value must be 6-30 characters long.",
						"constraints": {
							"required": true,
							"regex": "^[a-z0-9]{6,30}$",
							"validationMessage": "Only lowercase alphanumeric characters are allowed, and the value must be 6-30 characters long."
						},
						"visible": true
					},
					{
						"name": "title",
						"type": "Microsoft.Common.TextBox",
						"label": "Azure Vote Extension Title",
						"toolTip": "This will replace the title's value inside your values.yaml",
						"defaultValue": "Azure Vote App",
						"constraints": {
							"required": true,
							"regex": "^[a-z0-9A-Z ]{1,30}$",
							"validationMessage": "Only alphanumeric characters are allowed, and the value must be 6-30 characters long."
						}
					},
          {
            "name": "clusterName",
            "type": "Microsoft.Common.TextBox",
            "label": "Cluster Name",
            "toolTip": "This will replace the title's value inside your values.yaml",
            "defaultValue": "alluxio",
            "constraints": {
              "required": true,
              "regex": "^[a-z0-9A-Z ]{1,30}$",
							"validationMessage": "Only alphanumeric characters are allowed, and the value must be 6-30 characters long."
						
            }
          },
          {
            "name": "workerCount",
            "type": "Microsoft.Common.Slider",
            "label": "Worker Count",
            "toolTip": "This will replace the title's value inside your values.yaml",
            "min": 1,
            "max": 50,
            "defaultValue": 3
          },
          {
            "name": "licenseString",
            "type": "Microsoft.Common.PasswordBox",
            "label": "Alluxio License",
            "toolTip": "This will replace the title's value inside your values.yaml",
            "constraints": {
              "required": true
            }
          }
        ]
      }
    ],
    "outputs": {
      "location": "[location()]",
      "clusterName": "[steps('AlluxioClusterSettings').clusterName]",
      "workerCount": "[steps('AlluxioClusterSettings').workerCount]",
      "licenseString": "[steps('AlluxioClusterSettings').licenseString]",
      "extensionResourceName": "[steps('AlluxioClusterSettings').extensionResourceName]"
    }
  }
}

